import 'package:flutter/material.dart';

class healthjournal extends StatelessWidget {
    const healthjournal({super.key});
  
    @override
    Widget build(BuildContext context) {
        return Scaffold(
          appBar: AppBar(title: Text("Health Journal")
          ),
        );

    }

}