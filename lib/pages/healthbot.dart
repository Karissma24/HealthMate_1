import 'package:flutter/material.dart';

class healthbot extends StatelessWidget {
    const healthbot({super.key});
  
    @override
    Widget build(BuildContext context) {
        return Scaffold(
          appBar: AppBar(title: Text("Healthbot")
          ),
        );

    }

}