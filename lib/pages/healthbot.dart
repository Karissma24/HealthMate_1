import 'package:flutter/material.dart';

class HealthBot extends StatelessWidget {
    const HealthBot({super.key});
  
    @override
    Widget build(BuildContext context) {
        return Scaffold(
          appBar: AppBar(title: Text("Heailo!")
          ),
        );

    }

}