import 'package:flutter/material.dart';

class MyHealth extends StatelessWidget {
    const MyHealth({super.key});
  
    @override
    Widget build(BuildContext context) {
        return Scaffold(
          appBar: AppBar(title: Text("My Health")
          ),
        );

    }
}